# test/

libft のテスト資源ディレクトリ。**42 提出対象外**。

## ファイル

- `test_design.md` — テスト設計書 (subject / 要件書 / 設計書に準拠)
- (今後追加予定) `Makefile`, `test_main.c`, `test_part1.c`, `test_part2.c`, `test_part3.c`, `test_helpers.{c,h}`

## 注意事項

- このディレクトリは採点対象外 (subject II: テストは提出不要)。
- root の `Makefile` からは参照されない。
- `norminette libft.h ft_*.c` のようにワイルドカード指定で root のみ検査すること。
- 42 リポジトリにコミットする前に本ディレクトリを削除するか `.gitignore` に追加すること。

詳細は `test_design.md` を参照。
